# Terraform-GCP-Solutions

```shell
brew install awscli
brew reubsakk awscli
aws version # Check installation

# Configure your AWS profile
aws configure --profile terraform-mastery-profile

npm install --save-dev dotenv-cli
```